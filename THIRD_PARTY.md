# Third-party code and attribution

This repository is MIT-licensed (see [LICENSE](LICENSE)) for the **original** work: documentation, benchmark harnesses and probes, the launcher, the overlay installers, and the measurements. Everything below is derived from, redistributes, or was made possible by someone else's work, and stays under its own licence. If a credit is missing, that is an error: open an issue.

## The model and the checkpoints

| item | origin | licence |
|---|---|---|
| Qwen3.8-Flash-Next (the model) | Qwen team, Alibaba — https://huggingface.co/Qwen/Qwen3.8-Flash-Next | Qwen Community License 1.0 (`license: other`, `license_name: qwen-community-1.0`); the checkpoint's `LICENSE` file governs the weights |
| `qwen3.8-flash-next-exl3-3.05bpw` (served 2026-09-16 to 2026-09-18) and `…-2.05bpw` | EXL3 quantisations by **turboderp** — https://huggingface.co/turboderp/Qwen3.8-Flash-Next-exl3 (exllamav3 1.4.4 converter, `mul1` codebook, calibration 250 rows × 2048) | the model's licence; the quantisation is turboderp's work |
| `qwen3.8-flash-next-exl3-2.50bpw-r0b0tlab` (the served checkpoint since 2026-09-18) | EXL3 quantisation by **r0b0tlab** — https://huggingface.co/r0b0tlab/Qwen3.8-Flash-Next-EXL3-2.50bpw (routed experts at K = 2 / 3 / 4, 4-bit MTP layer) | the model's licence |

This repository ships no weights.

## The engine and the server (the image base)

| item | origin | licence | how it is used |
|---|---|---|---|
| ExLlamaV3 v1.5.0 | **turboderp** — https://github.com/turboderp-org/exllamav3 | MIT | the inference engine; every kernel patch below modifies its source and is a derivative work under MIT |
| TabbyAPI (commit `53da7919d4e45c63f4acbcbbc00cbe0f60a1ce65`) | **theroyallab** — https://github.com/theroyallab/tabbyAPI | **AGPL-3.0** | the OpenAI-compatible server; `docker/Dockerfile.tabbyapi` is adapted from their `docker/Dockerfile`; two patches below touch TabbyAPI files and are AGPL-3.0 derivatives |
| llguidance 1.8.0 | Microsoft — https://github.com/guidance-ai/llguidance | MIT | structured output (`json_schema`, `regex_pattern`) through TabbyAPI's exllamav3 grammar backend; unmodified |
| flash-linear-attention, PyTorch cu128, Triton | their respective projects | Apache-2.0 / BSD / MIT | pulled by TabbyAPI's `cu12` extra; unmodified |
| `nvidia/cuda:12.8.1-*-ubuntu24.04` | NVIDIA | NVIDIA Deep Learning Container licence | base images; nothing redistributed here |

## Upstream ExLlamaV3 pull requests carried in the served image

| file here | upstream | author | status | what it is |
|---|---|---|---|---|
| `docker/upstream-pr337-layer-split-device.patch` | [exllamav3#337](https://github.com/turboderp-org/exllamav3/pull/337) | **creslinux** | open (2026-09-17) | keeps the current CUDA device on the module's device during a layer-split forward; **verbatim redistribution**, applied by `docker/Dockerfile.tabbyapi-pr337` |
| `bench/hotvocab/hotvocab-applied.patch` | [exllamav3#303](https://github.com/turboderp-org/exllamav3/pull/303) | **keldenl** | open | "Speed up Qwen MTP decoding by 22% with a selected vocabulary head"; ported to this checkpoint's `qwen4_exp_mtp` by this repo. Measured **inapplicable** on a two-card layer split (`docs/MEASUREMENTS.md`, R377); not in the served image |
| the `gdn.cu` hunks of `docker/overlays/decode-kernels-r6/served-source.patch` | [exllamav3#369](https://github.com/turboderp-org/exllamav3/pull/369) | **troycheng** | closed without merge (2026-09-14) | "[kernel] Use one-warp blocks for GDN B/A decode on SM120"; **adapted, not verbatim**: the PR's kernel template on warps per block, its launch pair and its graph-parameter recording are kept, and the PR's fixed-shape condition (rows = 1, N = 96, K = 5,120) is replaced by "served grid below the SM count on SM 12.0" behind `EXL3_GDN_BA_WARP1`, to match this checkpoint's 4-row verify calls; served since R540 |

## Patches written for this repository (MIT for the patch text; derivative of ExLlamaV3 MIT / TabbyAPI AGPL-3.0 where they modify those files)

All of these were written with **OpenAI Codex** (`gpt-6-astra`, and `gpt-5.6-sol` from 2026-09-17 21:05 CEST) working from static source dumps, then built, measured and accepted or rejected on the box by the operator. Codex never ran on the GPU box; every number attached to them in `docs/MEASUREMENTS.md` is a box measurement. The design notes, audits and verifiers that came with each patch live next to it in the private working repository; the files here are the ones the served image was built from.

| file here | modifies | derived from | what it is |
|---|---|---|---|
| `docker/Dockerfile.tabbyapi` (the `sed` on `exllamav3/generator/job.py`) | ExLlamaV3 | MIT | the "R338 requeue token-count fix": `usage.completion_tokens` and the logged T/s under-counted by ~5× past the requeue boundary |
| `docker/qsa-multijob-applied.patch` | ExLlamaV3 (`exllamav3_ext/libtorch/attention.{cpp,h}`, `modules/attention_fn/{bc_attn,bc_dsa,bc_mla,dsa_triton,qsa_triton}.py`) | MIT | causal multi-job QSA sparse attention within the existing slot limits (the captured QSA path was single-job above its sparse threshold) |
| `docker/ci-depth-applied.patch` + `docker/tabby-split.patch` | ExLlamaV3 (`generator/{generator,job}.py`) **and TabbyAPI** (`common/config_models.py`, `backends/exllamav3/model.py`, `config_sample.yml`) | MIT + **AGPL-3.0** | concurrency-indexed MTP draft depth (`draft_num_tokens_by_batch`, the served policy `[[4, 3], [8, 1]]`) |
| `docker/bszn16.patch` | ExLlamaV3 (`modules/{block_sparse_mlp,mlp}.py`, `exllamav3_ext/libtorch/mlp.h`, `exllamav3_ext/quant/exl3_moe_coop{.cu,_kernel.cuh}`) | MIT | raises the fused decode-shaped MoE path's row limit `MAX_BSZN` 8 → 16 so batched MTP decode stays on the fast path |
| `docker/coopwide.patch` | ExLlamaV3 (`exl3_moe_coop.cu`) | MIT | selects the wide stage-B tile at ≥ 128 slots |
| `docker/hc-mix-v2.patch` + `docker/hc-mix-v2-r2.patch` | ExLlamaV3 (`exllamav3_ext/hc_mix.{cu,cuh}`, `bindings.cpp`, `modules/hyperconnections.py`) | MIT | bit-exact rewrite of the decode-form hyper-connection mixer kernels (`EXL3_HC_MIX_V2`): round 1 adds the V2 kernels beside the originals, round 2 (tiled kernels and the row gate `EXL3_HC_MIX_V2_MIN_R`) is written against round 1; applied in that order |
| `docker/hostgap-gated_delta_net.py` | ExLlamaV3 (`modules/gated_delta_net.py`, whole-file overlay) | MIT | removes host-side gaps in the GDN decode path (`EXL3_HOST_GAP_REWIND`) |
| `docker/prefill-pipeline.patch` | ExLlamaV3 (`generator/{generator,job}.py`, new `generator/prefill_pipeline.py`) | MIT | two-card prefill pipeline for the layer split (`EXL3_LS_PREFILL_PIPELINE`) |
| `docker/overlays/prefill-nosync-overlay` | ExLlamaV3 (`generator/*`, `modules/{block_sparse_mlp,moe_batch_recon}.py`, new `util/prefill_nosync.py`) | MIT | removes the blocking host syncs in the pipelined prefill |
| `docker/overlays/prefill-pipeline-mtp-overlay` | same files, cumulative | MIT | the pipeline's MTP eligibility fix (`install.py`, `payload/`) and free-VRAM guard fix (`memfix.py`, which rewrites the guard line of `generator/prefill_pipeline.py` so the 320 MB check also counts the caching allocator's reusable reserve; found and written for this repository while measuring the pipeline, R439 to R441) |
| `docker/overlays/moe-coop-v2-overlay` | ExLlamaV3 (`exl3_moe_coop.cu`, new `exl3_moe_coop_v2_kernel.cuh`, `comp_units/exl3_moe_coop_instances.cuh`) | MIT | bit-exact V2 of the fused MoE decode kernel: bounded work loops and batched completions (`EXL3_MOE_COOP_V2`) |
| `docker/overlays/decode-kernels-r2` | ExLlamaV3 (`exllamav3_ext/libtorch/blocksparse_mlp.{cpp,h}`, `exl3_moe_coop.{cu,cuh}`) | MIT | the shared expert on a side CUDA stream (`EXL3_SHARED_EXPERT_OVERLAP`) |
| `docker/overlays/refbase` | ExLlamaV3 (nine engine files) | MIT | the reference tree the next overlays were written against; both new switches off |
| `docker/overlays/decode-kernels-r4` | ExLlamaV3 (`generator/*`, `modules/hyperconnections.py`, `exllamav3_ext/*`) | MIT | pinned draft staging, batched verify, draft-head pruning (`EXL3_MTP_HEAD_N`), int8 mixer weights |
| `docker/overlays/prefill-e3-r2` | ExLlamaV3 (`modules/block_sparse_mlp.py`, `exllamav3_ext/quant/exl3_moe_prefill_e3*`, `bindings.cpp`) | MIT | grouped MoE prefill for K = 2 / 3 / 4 (`EXL3_MOE_PREFILL_E3`) |
| `docker/overlays/stack-r4-e3r2` | same files as the two above | MIT | both overlays in one image, with a merged `bindings.cpp` |
| `docker/overlays/mtp-pruned-r1/` | ExLlamaV3 (`generator/generator.py`, `architecture/qwen4_exp_mtp.py`, `modules/embedding.py`, new `modules/embedding_pruned.py`) | MIT | the MTP draft chain kept on the GPU with a 320 MiB copy of the 65,536 embedding rows the pruned draft head can emit; written for this repository by an Opus agent round (R522, served since R528) |
| `docker/overlays/tool-choice-r1/` | TabbyAPI (`endpoints/OAI/utils/chat_completion.py`, new `endpoints/OAI/utils/tool_choice.py`, tests) | AGPL-3.0 | `tool_choice: "required"` and named-function enforcement: an llguidance (Microsoft, MIT) Lark grammar that switches on when reasoning ends through TabbyAPI's existing `filter_trigger`, a call-only continuation when a forced turn ends in content, and a 503 when a forced turn ends without the call; written for this repository by an Opus agent round (R523, served since R529) |
| `docker/overlays/ple-ckpt-clone-r1/` | ExLlamaV3 (`modules/ple.py`) | MIT | a one-line change to `PLELayerState.stash()`: `.clone()` in place of `.cpu()` on the host-resident token-id window; found and written for this repository while testing the recurrent-tip checkpoint round (R524), served since R530 |
| `docker/overlays/nvme-tier-r4/` | ExLlamaV3 (`generator/generator.py`, `generator/pagetable.py`, `generator/async_generator.py`, `cache/recurrent.py`, new `generator/disk_cache.py`) | MIT | persistent NVMe prefix tier; written for this repository by agent rounds: Codex (gpt-5.6-sol) round 1, an omp agent round 2, Opus rounds 3 and 4 (R526, R532; served since R534) |
| `docker/overlays/e3-det-r1/` | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/quant/exl3_moe_prefill_e3.{cpp,cu,cuh}`, `modules/block_sparse_mlp.py`) | MIT | deterministic E3 grouped MoE prefill: per-assignment slots and a fixed-order reduction in place of `atomicAdd`, opt-in `EXL3_MOE_PREFILL_E3_DET=1`; written for this repository by an Opus agent round (R531, R533; served since R535) |
| `docker/overlays/decode-kernels-r6/` | ExLlamaV3 (`exllamav3_ext/gdn.cu`, `exllamav3_ext/hc_mix.{cu,cuh}`, `exllamav3_ext/bindings.cpp`, `modules/hyperconnections.py`) | MIT | one-warp launches of the GDN B/A projection GEMV (`EXL3_GDN_BA_WARP1`, adapted from exllamav3#369, see the upstream table above) and of `hc_apply` (`EXL3_HC_APPLY_WARP1`) when their served grid does not fill the card, and a re-gridded V2 mixer state kernel on the int8 path (`EXL3_GR_STATE_REGRID`), all keeping each output's arithmetic order; written for this repository: round 5 by Codex, rebased onto the served chain and ported to the int8 mixer as round 6 by an Opus agent round (R538; served since R540) |
| `docker/overlays/qsa-rawk-ring-r1/` | ExLlamaV3 (`cache/qsa.py`, `modules/qsa_indexer.py`, `modules/attention_fn/qsa_triton.py`, `modules/attention_fn/bc_attn.py`) | MIT | a 20-row ring per page for the QSA indexer's raw keys (`EXL3_QSA_RAWK_RING=1`), three Triton kernels next to the served ones with their arithmetic, a CPU interpreter of the Triton subset for the tests; the idea came from this project's VRAM census of the served daily; written for this repository by an Opus agent round (R544; served since R546) |
| `docker/overlays/gdn-state-bf16-r1/` | ExLlamaV3 (`exllamav3_ext/gdn.cu`, `modules/gated_delta_net.py`, `modules/gated_delta_net_fn/gated_delta_rule.py`) | MIT | the Gated-DeltaNet recurrent state stored in bf16 with fp32 math (`EXL3_GDN_STATE_BF16=1`): a state-type template parameter on both recurrent kernels, bf16 allocation of the per-slot state and its history, an fp32 copy for the chunked prefill; the idea came from this project's VRAM census of the served daily; written for this repository by an Opus agent round (R543; served since R548) |
| `docker/overlays/ngram-prefetch-r1/` | ExLlamaV3 `modules/ngram_embedding.py`, `modules/ple.py`, `generator/generator.py`, `generator/prefill_pipeline.py` | MIT | the per-layer n-gram embedding row reads started on the worker thread right after the MTP draft readback (`EXL3_NGRAM_PREFETCH2=1`), plus a wall-clock instrument of the exposed wait; the idea came from this project's decode profile (R465: 0.72 ms per step of inline row staging); written for this repository by an omp agent round with the `glm-5.3-flash` model, its GPU test harness fixed by this project (R559; served since R565) |
| `docker/overlays/mtp-kv-window-r2/` | ExLlamaV3 (`cache/cache.py`, `generator/generator.py`, `generator/job.py`, `generator/pagetable.py`, `modules/attention_fn/bc_attn.py`, new `cache/mtp_window.py`) | MIT | a windowed KV cache for the MTP draft layer (`EXL3_MTP_KV_WINDOW=<tokens>`): one sink page plus a per-slot ring of the last N tokens, with the QSA scan capped to the window; written for this repository by an omp agent round with the `glm-5.3-flash` model (r1), rebased by this project onto the R565 image as r2 (R569, R573; served since R579) |
| `docker/overlays/metrics-r1/` | TabbyAPI (`common/gen_logging.py`, `endpoints/core/router.py`, new `common/metrics.py`, new tests) | AGPL-3.0 | a Prometheus `/metrics` endpoint fed from TabbyAPI's generation log: generated-token and generate-seconds counters and histograms, standard library only; the metric set came from this project's measurements of production decode (R583, R585); written for this repository by a Devin agent round with the SWE-2 model (served since R587) |
| `docker/verifybatch-r1.patch`, `docker/Dockerfile.tabbyapi-bverify` | ExLlamaV3 (`generator/sampler/custom.py`, `generator/draft_overlap.py`, `generator/generator.py`) | MIT | makes the batched MTP verifier of `decode-kernels-r4` reachable: the sampler's resource flags aggregated over the simplified steps, and a request's device logit mask passed into the batched sampler call instead of vetoing batch mode; the cause came from this project's py-spy profile of the served generator (R645); written for this repository by a Devin agent round with the SWE-2 model (served since R646) |
| `docker/mtpnorm-r1.patch`, `docker/Dockerfile.tabbyapi-mtpnorm` | ExLlamaV3 (`modules/arch_specific/qwen4_exp_mtp.py`) | MIT | the MTP input layer's grouped RMS norm chain replaced by one call to ExLlamaV3's `ext.rms_norm` binding and a widening add; the candidate came from this project's per-step launch inventory (R648); written for this repository by a Devin agent round with the SWE-2 model (R649; served in `stack-r1` since R653) |
| `docker/mixstate-r1.patch`, `docker/Dockerfile.tabbyapi-mixstate` | ExLlamaV3 (`exllamav3_ext/hc_mix.{cu,cuh}`, `exllamav3_ext/bindings.cpp`, `modules/hyperconnections.py`) | MIT | a new `gr_v2_up_i8_state_kernel` that derives the gated-residual state row inside the int8 up kernel with the same intrinsics and accumulation order as the re-gridded state kernel of `decode-kernels-r6` (`EXL3_GR_STATE_IN_UP=1`); the candidate came from this project's per-step launch inventory (R648); written for this repository by a Devin agent round with the SWE-2 model (R651; served in `stack-r1` since R653) |
| `docker/prefbatch-r1.patch`, `docker/Dockerfile.tabbyapi-prefbatch` | ExLlamaV3 (`generator/generator.py`) | MIT | the MTP accept path's per-job draft prefills grouped by accepted length, one call per group under the windowed draft cache; the candidate came from this project's per-step launch inventory (R648); written for this repository by a Devin agent round with the SWE-2 model (R652; served in `stack-r1` since R653) |
| `docker/overlays/slotfix-r1/` | ExLlamaV3 (`cache/cache.py`, `generator/generator.py`) | MIT | exception safety for the recurrent-state slot pool: every allocation path returns the slot when the state constructor raises, `release_state` refuses a double release, `reap_failed_job` and `cancel()` release the draft window and the pages under separate handlers, and a file-armed fault injector for the gate; the cause came from this project's server logs of a 12-agent SWE-bench run (R586c); written for this repository by a Devin agent round with the SWE-2 model (served since R676) |
| `docker/overlays/hcfast-r1/` | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, new `exllamav3_ext/hc_mix_v3.{cu,cuh}`, `modules/hyperconnections.py`) | MIT | V3 of the decode-time int8 hyper-connection mixer kernels (`EXL3_HC_MIX_V3=1`, tile caps `EXL3_HC_MIX_V3_DOTS_B` / `_UP_B`): each int8 weight converted once per iteration, the loads batched ahead of the first multiply, the up reduction as a reduce-scatter; the same launches, grids and operand order as the kernels it replaces. `hc_mix_v3.cu` is new code written against turboderp's `hc_mix.cu` in ExLlamaV3 v1.5.0 as this repository's earlier mixer patches left it (the V2 kernels, int8 weights, the state row folded into the up kernel). The levers came from this project's source-level ncu of the served chain (R697) and its review; written for this repository by an Opus agent round (R698, R699; served since R701) |
| `docker/overlays/moefast-r1/` | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/quant/exl3_moe_coop.cu`, `exllamav3_ext/quant/comp_units/exl3_moe_coop_inst_k{2,3,4}.cu`, `exllamav3_ext/quant/comp_units/exl3_moe_coop_instances.cuh`, new `exllamav3_ext/quant/exl3_moe_coop_v3_kernel.cuh`) | MIT | V3 of the routed-expert cooperative MoE decode kernels (`EXL3_MOE_COOP_V3=1` or `2`): a cp.async weight ring with a counted wait, a one-slice activation prefetch, one acquire/release counter arrival per item, and in mode 2 the narrow down stage merged per 128-column chunk; storage-bit identical to V2. The kernel is derived from turboderp's `exl3_moe_coop` kernels in ExLlamaV3 v1.5.0 by way of this repository's V2 (`moe-coop-v2-overlay`). The levers came from this project's source-level ncu of the served kernels (R696) and its review; `d0/d0_microbench.py` is this project's kernel microbenchmark; written for this repository by an Opus agent round (R700b; served since R701) |
| `docker/overlays/hcfast-r2/` (in the served image as `docker/overlays/stack-r3/series/01-hcfast-r2-on-stack-r2.patch`) | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/hc_mix_v3.{cu,cuh}`, `modules/hyperconnections.py`) | MIT | round 2 of the int8 hyper-connection mixer kernels (`EXL3_HC_MIX_V3=2`): batched per-row stream loads in dots, a branch-free up prelude with its loads issued first, per-row-count tile tables and a dots → up programmatic-launch switch; derived from hcfast-r1 and, through it, from turboderp's `hc_mix.cu`. The levers came from this project's R698 P0 sweep and the review of its R697 source-level ncu; written for this repository by an Opus agent round (R702; served since R716b) |
| `docker/overlays/latchain-r1/` (in the served image as `docker/overlays/stack-r3/series/02-latchain-r1b.patch`, one `bindings.cpp` line re-anchored) | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/gdn.cu`, `exllamav3_ext/libtorch/{attention.cpp,attention_bc.h,gated_delta_net.cpp}`, new `exllamav3_ext/libtorch/lc_fork.h`, `exllamav3_ext/quant/exl3_gemm.{cu,cuh}`, `modules/attention_fn/bc_attn.py`) | MIT | a register-resident variant of turboderp's Gated-DeltaNet recurrent decode kernel (`EXL3_LC_GDN_RR=1`), the QSA indexer chain and the GDN b/a GEMV captured as parallel CUDA-graph branches (`EXL3_LC_QSA_FORK=1`, `EXL3_LC_GDN_FORK=1`), a lock-region offset for the branch GEMM, and compile options for the QSA sparse split and combine Triton kernels (`EXL3_LC_QSA_*`); the chains came from this project's R680 decode traces and its per-kernel roofline gap table; written for this repository by an Opus agent round (R712; RR, QF and the QSA options served since R716b, GF not served) |
| `docker/overlays/moefast-r3/` (in the served image as `docker/overlays/stack-r3/series/03-moefast-r3.patch`, verbatim) | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/libtorch/blocksparse_mlp{.cpp,.h,_bc.h}`, `exllamav3_ext/quant/exl3_moe_coop.{cu,cuh}`, `exllamav3_ext/quant/comp_units/exl3_moe_coop_inst_k{2,3,4}.cu`, `exllamav3_ext/quant/comp_units/exl3_moe_coop_instances.cuh`, new `exllamav3_ext/quant/exl3_moe_coop_r2_kernel.cuh`, `modules/block_sparse_mlp.py`) | MIT | the r2 routed-expert MoE decode kernels (`EXL3_MOE_COOP_V3=3`, with a per-row-count mode map) and the shared expert forked onto its side stream before the router (`EXL3_SHARED_EXPERT_EARLY=1`); the kernels derive from turboderp's `exl3_moe_coop` kernels by way of this repository's V2 and V3 (`moe-coop-v2-overlay`, `moefast-r1`). The early fork came from this project's nsys timelines of the r2 gate (R703b); written for this repository by Opus agent rounds, moefast r2 and r3 (R713; served since R716b) |
| `docker/overlays/densegemm-r1/`, `docker/overlays/densegemm-r2/` (r2 in the served image as `docker/overlays/stack-r3/series/05-densegemm-r2.patch`, verbatim) | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/quant/{exl3_gemm.cu,exl3_gemv.cu,coop_autotune.cu,coop_autotune.cuh}`, new `exllamav3_ext/quant/{exl3_dense_v2.cu,exl3_dense_v2.cuh,exl3_gemm_inner_v2.cuh,exl3_gemm_v2_kernel.cuh,exl3_gemv_v2_kernel.cuh}`, new `exllamav3_ext/quant/comp_units/exl3_dense_v2_inst_{gemm,gemv,mgemm}.cu`) | MIT | V2 twins of turboderp's dense EXL3 decode kernels `exl3_gemm`, `exl3_mgemm` and `exl3_gemv`, reusing the served autotuned configuration per call (`EXL3_DENSE_V2=1`, `2` or `3`, and `EXL3_DENSE_ROWS32=1`): weight loads issued before the input Hadamard barrier, a gathered stream-K fixup, fewer barriers, a warp-private cp.async ring in the gemv. The levers came from this project's R680 traces and R682 kernel microbenchmark; r2's fix for r1's local-memory stack frames came from this project's R710 resource-usage dump. Written for this repository by Opus agent rounds, r1 and r2 (R710b, R714; r2 mode 1 served since R716b) |
| `docker/overlays/stack-r3/` (`series/06-dense-lcguard.patch`, `series/apply-series.sh`, `series/check-subsets.sh`, `install-stack.sh`, `tools/`, `tests/test_dense_lcguard_cpu.py`) | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/quant/exl3_dense_v2.{cu,cuh}`) | MIT | the stack-r3 patch series and its installer; the guard gives a dense V2 call on latchain's QSA side branch its own gemv counters and gemm scratch, detected by the branch's lock offset. The race it closes was found in this project's reviews of R712 and R710b; the multi-stream greedy rule in `tools/greedy_streams.py` was rewritten after this project's review of R716b. Written for this repository by an Opus agent round (served since R716b) |
| `docker/overlays/rows32-r4/` | ExLlamaV3 (`exllamav3_ext/bindings.cpp`, `exllamav3_ext/libtorch/{blocksparse_mlp.cpp,mlp.cpp,mlp.h}`, `exllamav3_ext/quant/{exl3_dense_v2.cu,exl3_moe_coop.cu,exl3_moe_coop.cuh}`, `modules/block_sparse_mlp.py`, `modules/mlp.py`) | MIT | host code that lets MTP verify batches of 17 to 32 rows stay on the cooperative decode paths: the routed-expert MoE decode in one launch up to 32 rows (`EXL3_MOE_COOP_ROWS32=1`) through the V2 and V3 cooperative kernels, which derive from turboderp's `exl3_moe_coop` kernels by way of this repository's `moe-coop-v2-overlay` and `moefast-r1`; the shared expert's `BC_GatedMLP` graphs and statics sized for 32 rows (`EXL3_SHARED_EXPERT_ROWS32=1`); a bound check on moefast r3's `start_shared`; and a 4 MiB side region for densegemm r2's ROWS32 twins (`EXL3_DENSE_ROWS32=1`, listed under densegemm above) on latchain's QSA branch. No device code changes: the SASS of every function is unchanged. The case for lifting the 16-row cap came from this project's R560 and R562 measurements of depth 2 above 16 rows and R708's unique-expert capture; r4 is r3 rebased on `stack-r3`. Written for this repository by Opus agent rounds, rows32 r3 and r4 (R717; served since R717c). Earlier rounds of the same change, r1 (a Devin agent round with the SWE-2 model) and r2, are not in this repository |

Each overlay's `manifest.json` pins the SHA-256 of the file it replaces and of the file it installs, and `install.py` refuses to run on a base whose files do not match.

## Ideas and techniques credited (no code copied)

| source | what was taken |
|---|---|
| DominikBucko — https://github.com/DominikBucko/qwen38-flash-next-2x3090 (Apache-2.0) | the same model on two RTX 3090s under vLLM: its exact top-k QSA scratch, 64 MiB score chunks, fused PLE RMSNorm, MTP fixes and dynamic speculative schedule were ported as opt-in experiments on the **vLLM + EXL3** audition track, not into the served ExLlamaV3 image; credited here because that track's measurements are referenced from `docs/` |
| HaberstrohSystems — https://github.com/HaberstrohSystems/qwen3.8-flash-next-24gb-sglang and https://huggingface.co/HaberstrohSystems/Qwen3.8-Flash-Next-int2-mixed-AutoRound-24GB-SGLang | the INT2 AutoRound checkpoint and the 2-bit `moe_wna16` SGLang path were assessed as a route for this box; nothing was ported |
| vcruz305 — https://github.com/vcruz305/vllm-exl3 | the vLLM plugin that loads EXL3 checkpoints, the basis of the vLLM + EXL3 audition track (its patch series is not in this repository) |
| vcruz305 — https://github.com/vcruz305/Qwen3.8-Flash-Next-EXL3-DGX-Spark-recipe | the pruned draft head (`EXL3_MTP_HEAD_N=65536`) and int8 hyper-connection mixer weights, both measured here in R499 |
| peonist-ai — https://github.com/peonist-ai/halogen (0.6.0) | prompt lookup beside the MTP draft, measured here in R501 |
| **plotarmordev** — [MiaAI-Lab/GLM-5.3-Flash-EXL3-2x-DGX-Sparks#217](https://github.com/MiaAI-Lab/GLM-5.3-Flash-EXL3-2x-DGX-Sparks/pull/217) "EXL3 thin-decode fast path" | the SASS audit for local-memory spills in the fused MoE decode kernel, and re-staging to remove them; applied here to the K=3 instances of the MoE coop V2 kernel, measured bit-exact and not faster in R536, not served |

## Benchmark and evaluation tools

| tool | origin | licence | use |
|---|---|---|---|
| lm-evaluation-harness (`lm_eval`, GSM8K 5-shot, n=200) | EleutherAI — https://github.com/EleutherAI/lm-evaluation-harness | MIT | quality gate |
| tool-eval-bench v2.1.0 | **SeraphimSerapis** — https://github.com/SeraphimSerapis/tool-eval-bench | MIT | agentic tool-call gate (69 cases × 4) |
| mini-SWE-agent + SWE-bench Verified | SWE-agent / princeton-nlp — https://github.com/SWE-agent/mini-swe-agent | MIT (harness) / SWE-bench data licence | the SWE-bench measurements in `bench/results/r359-swebench.md` |
| `vllm bench serve` (vLLM v0.30.0, image `vllm/vllm-openai:v0.30.0`) | vLLM project — https://github.com/vllm-project/vllm | Apache-2.0 | the standard serving benchmark in `bench/results/r731b-std-bench.md`; run unmodified except for the functions `bench/vllm_bench_tabby.py` replaces at import |
| `bench/vllm_bench_tabby.py` | this repository; its `tabby_chat` is a modified copy of vLLM v0.30.0's `async_request_openai_chat_completions` (`vllm/benchmarks/lib/endpoint_request_func.py`), and its Spec-Bench reader re-implements `SpecBench.load_data` without pandas | Apache-2.0 for the parts derived from vLLM, MIT otherwise | makes `vllm bench serve` measure TabbyAPI correctly: usage read from any frame, CRLF-separated events, `min_tokens` for length forcing, Spec-Bench prompts templated once, a per-cell sample manifest |
| ShareGPT V3 (`ShareGPT_V3_unfiltered_cleaned_split.json`) | anon8231489123 — https://huggingface.co/datasets/anon8231489123/ShareGPT_Vicuna_unfiltered | Apache-2.0 (dataset card) | prompts and reference output lengths for the standard benchmark; not redistributed here |
| Spec-Bench (`data/spec_bench/question.jsonl`) | **hemingkx** — https://github.com/hemingkx/Spec-Bench | Apache-2.0 | the 480 prompts of the standard benchmark; not redistributed here |
| `bench/*.py` probes (`probe.py` = `fn_bench`, `needle.py`, `capabilities.py`, `summarize.py`, `multiprompt.py`, `agentic-edit.py`, `nostop_proxy.py`, `agent_replay.py`, `revisit.py`, `std_bench_summary.py`) | this repository | MIT | original |
